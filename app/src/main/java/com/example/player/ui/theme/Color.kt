package com.example.player.ui.theme

import androidx.compose.ui.graphics.Color

// 品牌色
val PrimaryBlue = Color(0xFF1565C0)

// 米黄色背景系
val DarkBackground  = Color(0xFFF5F0E8)   // 淡米黄色主背景
val DarkSurface     = Color(0xFFEDE8DC)   // 稍深米黄，用于搜索框等表面
val DarkCard        = Color(0xFFE8E3D7)   // 卡片背景
val DarkBorder      = Color(0x14000000)   // 8% black，主要分割线
val DarkBorderLight = Color(0x1F000000)   // 12% black，细节边框

// 文字（深色，在米黄背景上可读）
val TextPrimary   = Color(0xFF1A1A1A)
val TextSecondary = Color(0xFF6B6560)
val TextTertiary  = Color(0xFF9A9590)

// 胶囊 Chip
val ChipSelected   = Color(0xFF1A1A1A)
val ChipUnselected = Color(0x14000000)

// 底部导航栏
val NavBarBg = Color(0xF0EDE8DC)
